<block wx:if="{{!isHis}}">
	暂无内容
</block>
<view wx:else>
	<view class="handleBtn" bindtap="handleBtn" hidden="{{!isHis}}">
		<button size="mini" plain>清空历史记录</button>
	</view>
	<photos collectionList="{{collectionList}}" type="hisUrl" bindmyevent="onMyEvent"></photos>
</view>